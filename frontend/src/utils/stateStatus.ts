const idleState: string = 'idle';
const loadingState: string = 'loading';
const failedState: string = 'failed';
const succeededState: string = 'succeeded';

export default {
    idleState,
    loadingState,
    failedState,
    succeededState,
}
